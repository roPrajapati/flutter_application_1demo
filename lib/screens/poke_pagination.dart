import 'package:flutter/material.dart';

class PokePaginationScreen extends StatelessWidget {
  const PokePaginationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Pagination api call"),),
      body: ListTile(
        title: Text("ttitl"),
        subtitle: Text("skjgfgsjdg"),
      ),
    );
  }
}